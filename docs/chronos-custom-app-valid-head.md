# Chronos custom-App valid-head probe

This inert documentation-only change gives the valid pull-request case a unique
Git commit and tree. It does not alter experiment source, policy, tests, or CI.

The repository-owned Chronos policy must pass on this head before the custom App
may publish a successful check.
